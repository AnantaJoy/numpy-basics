# import numpy using alias
import numpy as np

# check version of numpy
print(np.__version__)

# create a numpy array and the type of that array 
arr = np.array([1, 2, 3, 4, 5, 6])
print(arr)
print(type(arr))
